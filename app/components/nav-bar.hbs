<nav class="menu">
    <div class="links">
        <LinkTo @route="home" class="menu-index">
            Home
        </LinkTo>
        <LinkTo @route="about" class="menu-about">
            About Us
        </LinkTo>
        <LinkTo @route="contact" class="menu-contact">
            Contact Us
        </LinkTo>
        {{#if this.session.isAuthenticated}}
            <LinkTo @route="logout" {{on "click" this.logout}}>Logout</LinkTo>
        {{else}}
            <LinkTo @route="login" class="menu-login">
                Login
            </LinkTo>
        {{/if}}
        <LinkTo @route="bookmark" class="menu-bookmark">
            Bookmarks
        </LinkTo>
    </div>
</nav>
